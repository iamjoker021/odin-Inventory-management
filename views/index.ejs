<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ODIN | Inventory Management App</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <h1>Inventory Management App</h1>
    <nav>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/add-category">Add New Category</a></li>
        </ul>
    </nav>
    <ul>
        <% for (let i=0; i<categoryList.length; i++) { %>
            <li>
                <a href="/category/<%= categoryList[i].id %>">
                    <div>
                        <h2><%= categoryList[i].name %></h2>
                        <a href="/edit-category/<%= categoryList[i].id %>">Edit</a>
                        <a href="/delete-category?id=<%= categoryList[i].id %>">Delete</a>
                    </div>
                </a>
            </li>
        <% } %>
    </ul>
</body>
</html>