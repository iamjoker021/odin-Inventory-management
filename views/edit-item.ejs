<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ODIN | Inventory Management App</title>
</head>
<body>
    <h1>Inventory Management App</h1>
    <nav>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/category/<%= category.id %>">Back to Category</a></li>
        </ul>
    </nav>
    <form action="/category/<%= category.id %>/edit-item/<%= item.id %>" method="POST">
        <label for="">item ID: </label>
        <input type="number" name="item-id" value="<%= item.id %>" disabled>
        <br>
        <label for="item-name">Item Name: </label>
        <input type="text" name="item-name" id="item-name" value="<%= item.name %>">
        <br>
        <label for="item-price">Item Price: </label>
        <input type="number" name="item-price" id="item-price" step="0.01" value="<%= item.price %>" >
        <br>
        <button type="Sumbit">Update item</button>
    </form>
</body>
</html>